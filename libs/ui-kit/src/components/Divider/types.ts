import type { DividerProps as BaseDividerProps } from 'antd';
import type { BordersProps, LayoutProps, SpaceProps } from 'styled-system';

export interface DividerProps extends BaseDividerProps, SpaceProps, LayoutProps, BordersProps {}
